name: GDM57 top-two ambiguity representation
on:
  push:
    branches: [main]
    paths:
      - experiments/followup57/**
      - .github/workflows/measured-followup57.yml
  workflow_dispatch:
    inputs:
      seeds:
        default: '5701,5702'
        type: string
permissions:
  contents: read
concurrency:
  group: graph-model-gdm57-measured
  cancel-in-progress: false
defaults:
  run:
    shell: bash
env:
  APOLLO_ELV2_LICENSE: accept
  APOLLO_TELEMETRY_DISABLED: 'true'
  HF_HUB_DISABLE_TELEMETRY: '1'
  TOKENIZERS_PARALLELISM: 'false'
  OMP_NUM_THREADS: '1'
  MKL_NUM_THREADS: '1'
  OMP_DYNAMIC: 'FALSE'
  MKL_DYNAMIC: 'FALSE'
  MKL_CBWR: COMPATIBLE
  ATEN_CPU_CAPABILITY: default
  PYTHONHASHSEED: '0'
  PYTHONUNBUFFERED: '1'
  SEEDS: ${{ inputs.seeds || '5701,5702' }}
jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install pinned CPU dependencies and Rover
        run: |
          set -euo pipefail
          mkdir -p artifacts "$HOME/.local/bin" /tmp/rover-install
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch==2.8.0
          python -m pip install -r experiments/measured/requirements.txt
          curl -fL --retry 3 https://github.com/apollographql/rover/releases/download/v0.41.0/rover-v0.41.0-x86_64-unknown-linux-gnu.tar.gz -o /tmp/rover.tar.gz
          tar -xzf /tmp/rover.tar.gz -C /tmp/rover-install
          find /tmp/rover-install -type f -name rover -exec install -m 755 '{}' "$HOME/.local/bin/rover" \;
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Compile and test GDM57 representation and inherited contracts
        run: |
          set -euo pipefail
          python -m compileall -q experiments/followup57
          python -m unittest experiments.followup57.test_followup experiments.followup56.test_followup experiments.followup55.test_followup experiments.followup54.test_followup experiments.followup53.test_followup experiments.followup52.test_followup experiments.followup51.test_followup experiments.followup50.test_cache_repair -v 2>&1 | tee artifacts/tests.log
      - name: Train and independently verify semantic-representation smoke policy
        run: |
          set -euo pipefail
          GITHUB_ACTIONS=true python -m experiments.followup57.execute --backbone hash --task operation --arms symmetric-request --seeds 5701 --epochs 2 --output artifacts/smoke 2>&1 | tee artifacts/smoke.log
          python - <<'PY'
          from pathlib import Path
          import json
          from experiments.followup57.collect import verify_summary

          summaries=list(Path('artifacts/smoke').rglob('summary.json'))
          assert len(summaries)==1, summaries
          summary=verify_summary(summaries[0])[0]
          h=summary['encoder']['head_training_reproducibility']
          assert h['adamw_foreach'] is False and h['adamw_fused'] is False
          assert h['clip_grad_norm_foreach'] is False
          assert h['moment_update_dtype']=='float32'
          assert h['final_parameter_application_dtype']=='float64->float32'
          assert h['stored_parameter_dtype']=='float32'
          assert h['feature_quantum_unchanged']==1e-4
          assert h['gdm57_execution_scope']=='capability_and_ambiguity_heads'
          assert h['canonical_gdm56_source_commit']=='168317f6495b05e04597ae084b75be6890fe0621'
          assert h['gdm57_change_scope']=='ambiguity-head top-two semantic input representation only; fixed canonical GDM56 family-balanced curriculum, numerical path and GDM54 5pp rescue arbitration'
          tr=summary['training']
          assert tr['gdm57_representation_mode']=='symmetric-request'
          assert tr['gdm57_ambiguity_feature_dim']==1291
          assert tr['gdm57_candidate_semantic_block'] is True
          assert tr['gdm57_request_semantic_block'] is True
          assert tr['gdm57_order_invariant_candidate_block'] is True
          assert tr['gdm56_ambiguity_curriculum']=='family-balanced'
          families=tr['gdm56_positive_family_counts']
          assert set(families)=={'role','lifecycle-time','representation','object-vs-supplier'}
          assert max(families.values())-min(families.values()) <= 1
          assert tr['gdm56_counterfactual_negative_fraction']==0.0
          cal=json.loads((summaries[0].parent/'calibration.json').read_text())['selected']
          assert cal['threshold_selection']=='ambiguity-rescue'
          assert cal['status_arbitration']=='ambiguity-rescue'
          assert cal['nomatch_recall_budget_pp']==5
          assert cal['nomatch_recall_floor'] <= cal['joint_baseline_validation_metrics']['nomatch_recall']
          assert cal['accepted_status_recall_floor'] <= cal['joint_baseline_validation_metrics']['accepted_status_recall']
          holdout=summary['secondary_holdout_ambiguity_family_metrics']
          assert set(holdout)=={'role','lifecycle-time','representation','object-vs-supplier'}
          print('gdm57 deterministic smoke evidence, matched-capacity semantic representation and fixed curriculum/arbitration contract verified')
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gdm57-preflight-${{ github.run_id }}
          path: artifacts/
          if-no-files-found: error
          retention-days: 14
  experiments:
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 240
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        task: [operation, schema]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: gdm57-hf-${{ runner.os }}-distilbert-v1
      - name: Install CPU model dependencies and Rover
        run: |
          set -euo pipefail
          mkdir -p artifacts "$HOME/.local/bin" /tmp/rover-install
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch==2.8.0
          python -m pip install -r experiments/measured/requirements.txt
          curl -fL --retry 3 https://github.com/apollographql/rover/releases/download/v0.41.0/rover-v0.41.0-x86_64-unknown-linux-gnu.tar.gz -o /tmp/rover.tar.gz
          tar -xzf /tmp/rover.tar.gz -C /tmp/rover-install
          find /tmp/rover-install -type f -name rover -exec install -m 755 '{}' "$HOME/.local/bin/rover" \;
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          python -m pip freeze > artifacts/packages.txt
      - name: Train ambiguity representations, execute GraphQL, compose schemas, and measure
        env:
          TASK: ${{ matrix.task }}
        run: |
          set -euo pipefail
          GITHUB_ACTIONS=true python -m experiments.followup57.execute --backbone distilbert --task "$TASK" --seeds "$SEEDS" --epochs 12 --output artifacts/results 2>&1 | tee artifacts/run.log
          test -s artifacts/results/worker.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gdm57-worker-${{ github.run_id }}-${{ matrix.task }}
          path: artifacts/
          if-no-files-found: error
          retention-days: 14
          compression-level: 3
  verify-batch:
    needs: [preflight, experiments]
    if: always() && needs.preflight.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/download-artifact@v4
        with:
          pattern: gdm57-worker-${{ github.run_id }}-*
          path: collected
      - name: Independently verify GDM57 evidence and reproduce metrics
        run: |
          set -euo pipefail
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch==2.8.0
          python -m pip install -r experiments/measured/requirements.txt
          python -m experiments.followup57.collect collected --seeds "$SEEDS" 2>&1 | tee collection.log
          test -s collected/BATCH_REPORT.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gdm57-batch-report-${{ github.run_id }}
          path: |
            collected/BATCH_REPORT.json
            collection.log
          if-no-files-found: error
          retention-days: 14
